[package]
name = "bootstrap"
version = "0.1.0"
authors = ["Jerome Rasky <jyrome.112@gmail.com>"]

[lib]
crate-type = ["staticlib"]

[dependencies]
rlibc = "*"
constants = { path = "../constants" }
serial = { path = "../serial" }
memory = { path = "../memory" }
log = { path = "../log" }
